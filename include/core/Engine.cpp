#include "Engine.h"
#include "log/log.h"
#include <GLFW/glfw3.h>
#include <vulkan/vulkan.h>

class engine
{
private:
    /* data */
public:
    engine(){
        log L;
    }
    void engineRun(int type){

        if (type == 0)
        {
            
        }
        
    }
    ~engine();
};
