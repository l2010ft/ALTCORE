#include <thread>
#include <queue>
#include <condition_variable>
#include "core/config.h"

class scheduler
{
private:
public:
    scheduler();
    class Physics
    {
    private:
        /* data */
    public:
        Physics(/* args */);
        ~Physics();
    };
    class Scripts
    {
    private:
        /* data */
    public:
        Scripts(/* args */);
        ~Scripts();
    };

    ~scheduler();
};
