#include "log/log.h"
#include "config.h"
class engine
{
private:
    log L;
public:
    engine();
    void enginestart(API type);
    ~engine();

    class make
    {
    private:
        /* data */
    public:
    }; 
};
